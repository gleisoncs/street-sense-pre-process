# Copy to .env and fill in.
KAFKA_BROKERS=localhost:9092
INPUT_TOPIC=filter1
OUTPUT_TOPIC=topic1
DEFAULT_MODEL=train4/weights/best.pt
# Fraction of GPS points required inside the region (1.0 = strict, all points).
MIN_INSIDE_RATIO=1.0
